[project]
name = "illusion-core"
version = "1.5.0"
description = "Shared helpers and wire types for illusion, claws and lipgloss"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28",
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
